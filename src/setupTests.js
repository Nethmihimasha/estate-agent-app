/**
 * Test setup file. This can be used to configure the testing environment
 * (e.g. extend expect with custom matchers or configure global mocks).
 *
 * Currently left intentionally minimal for this coursework, but kept in
 * place so Jest/React Testing Library can load any future configuration.
 */
